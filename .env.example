# Proposed configuration names only; no application consumes this file yet.
TTX_HOST=127.0.0.1
TTX_PORT=3000
# Required later: approved absolute directory outside Git and OneDrive.
TTX_DATA_DIR=
# First slice has no AI calls.
TTX_AI_ENABLED=false
# Keep unset until provider and data permissions are established.
TTX_AI_ENDPOINT=
TTX_AI_API_KEY=
